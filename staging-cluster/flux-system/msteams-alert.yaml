apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Alert
metadata:
  name: azure-vote-front
  namespace: flux-system
spec:
  providerRef:
    name: msteams
  eventSeverity: info
  eventSources:
    - kind: GitRepository
      name: '*'
    - kind: Kustomization
      name: '*'